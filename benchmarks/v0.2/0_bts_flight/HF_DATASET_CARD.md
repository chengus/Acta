# Hugging Face dataset card source

The dataset repository now contains multiple Acta v0.2 benchmarks. Its
canonical combined dataset card moved to
[`../HF_DATASET_CARD.md`](../HF_DATASET_CARD.md).

This pointer remains at the original BTS-specific location so older references
lead to the current card instead of a stale BTS-only copy.
